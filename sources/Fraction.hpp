

namespace ariel{

    class Fraction
    {
    private:
        int numerator;
        int denominator;

    public:
        Fraction(/* args */);
        Fraction(int , int);
        ~Fraction();
    };
    
    Fraction::Fraction(/* args */)
    {
    }
    
    Fraction::~Fraction()
    {
    }
    
}